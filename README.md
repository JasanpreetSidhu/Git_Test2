# Useful Git Commands

## ==git add --all==
> To add all modified files to staging area
>

**bold text**
*italicized text*

## git init
## git add README.md
## git commit -m "first commit"
git branch -M main

## git remote remove origin
> used to remove origin if origin already exists in your local git configuration
>


git remote add origin https://github.com/Jasanpreet5/New.git
git push -u origin master
…or push an existing repository from the command line
git remote add origin https://github.com/Jasanpreet5/New.git
git branch -M master
git push -u origin master


## ==git checkout -b branch_new==
> creates a local branch and switch to that branch
>

## git branch -d branch_new
> deletes a local branch
>

## git push origin more_commands
> to create remote branch by pushing local branch
>

## git merge more_commands
> merging changes from more_commands branch to the working branch
>

## git diff
> shows changes between current files and staged area
>

## git diff --staged
> *shows changes between staged files and commits*
>

# Steps to use git and github

## Create a local branch

## Push Changes
> Push branch to github repository after finishing with your component
>


## Open a pull request
> proposing changes to base or master branch
>


## Discussion and Review
> review commits
>

## Merge
> Merge the branch to base or master branch
>



# Git Commands Continued

## git commit --help
> loads command manual in browser with full explanation of all the options
>







